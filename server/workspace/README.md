# Workspace

This is the agent's workspace root. The CODE agent can read, search, and (in later phases) write files here.

Drop your project files into this directory, or point `WORKSPACE_DIR` in `server/.env` at any directory on your machine. The agent cannot access anything outside this root.
